services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma  # ← Data stored here on your laptop

volumes:
  chroma-data:  # ← This is a Docker volume on your machine
